% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{normalize_matrix}
\alias{normalize_matrix}
\title{Normalize a count matrix with sparse PFlog}
\usage{
normalize_matrix(
  counts,
  target = "auto",
  alpha = NULL,
  log1p = TRUE,
  center = TRUE
)
}
\arguments{
\item{counts}{Count matrix with features in rows and cells in columns.}

\item{target}{\code{"auto"}, \code{"mean"}, \code{"median"}, or a numeric fixed \code{K}.}

\item{alpha}{Optional overdispersion value. If supplied, it overrides
\code{target} and uses \code{K = 4 * alpha * mean_depth}.}

\item{log1p}{Whether to use the shifted logarithm.}

\item{center}{Whether to return and use the row/cell centering vector.}
}
\value{
A list with \code{sparse}, \code{center}, \code{k}, and \code{alpha}.
}
\description{
Returns the sparse shifted-log values and the per-cell centering vector. The
dense normalized matrix is represented implicitly as \code{sparse - center}.
}
