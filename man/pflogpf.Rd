% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat.R
\name{pflogpf}
\alias{pflogpf}
\title{Add sparse PFlogPF normalization to a Seurat object}
\usage{
pflogpf(
  object,
  assay = NULL,
  layer = "counts",
  target = "auto",
  alpha = NULL,
  key.added = "pflogpf",
  center.key = paste0(key.added, "_center"),
  log1p = TRUE
)
}
\arguments{
\item{object}{A Seurat object.}

\item{assay}{Assay name. Defaults to \code{DefaultAssay(object)}.}

\item{layer}{Input layer, usually \code{"counts"}.}

\item{target}{\code{"auto"} to estimate alpha and set \code{K = 4 * alpha * mean_depth}, \code{"mean"}, \code{"median"}, or a numeric fixed \code{K}.}

\item{alpha}{Optional overdispersion value, overriding \code{target}.}

\item{key.added}{Name of the assay layer that will hold sparse shifted-log
values.}

\item{center.key}{Metadata column for the per-cell centering vector.}

\item{log1p}{Whether to use shifted log.}
}
\value{
The modified Seurat object.
}
\description{
\code{pflogpf()} reads a Seurat assay layer, computes PFlogPF using Rust, stores
the sparse shifted-log values as a new assay layer, and stores the per-cell
centering vector in \code{object[[]]}. Downstream PCA can then use the implicit
matrix \code{layer - center} without densifying.
}
